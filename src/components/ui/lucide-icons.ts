
import { ShieldPlus, UserCheck } from 'lucide-react';

// This is a placeholder file to hint to the bundler about dynamically used icons.
// You can ignore this file. It's a workaround for tree-shaking issues.
const LucideIcons = {
  ShieldPlus,
  UserCheck
};

export default LucideIcons;
